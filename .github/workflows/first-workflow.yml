name: first workflow
on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
    branches:
      - dev-1
  push:
    branches:
      - master
jobs:
  my-only-job:
    runs-on: ubuntu-latest
    steps:
    - name: The Only step
      run: |
        echo "The workflow has been triggered successfully"
        echo "move on to the next task"